[
  {
    "cap": "persp.camera.identity",
    "inject": { "kind": "transform", "camera": true, "position": { "x": 0 } },
    "time": 0.0, "min_psnr": 30, "expect_identity": true,
    "note": "Floor only: with a perspective Camera the base drop-zone must still render == imageA in BOTH engines (no fold => headless==GUI here, VALID). Guards the harness. PASS 43.3."
  },
  {
    "cap": "persp.NOTE.no_synthetic_fold_caps",
    "inject": { "kind": "transform", "position": { "x": 0 } },
    "time": 0.0, "min_psnr": 0, "oracle": "INVALID_HEADLESS_NE_GUI",
    "note": "⛔ NO synthetic bare-camera FOLD caps here (removed 2026-07-23). A Camera + NO-Replicator plane-fold is composited ORTHOGRAPHICALLY by the GUI (the ONE truth) while headless FCP folds perspectively — proven by OZViewer::viewIsOrthographic + the exhaustive ROADMAP Reflection/Color_Planes measurement (perspective is a DEAD-END vs GUI for those slugs). So a synthetic bare-camera fold cap tests against the WRONG truth. A VALID perspective oracle needs GENUINELY-3D content where GUI==headless: the camera+clone slug 3D_Rectangle (headless 32 dB vs GUI = trustworthy). That is wired as the perspective subsystem's minimized oracle (Perspective__3D_Rectangle_cam), NOT a synthetic cap. This entry is a documentation marker (SKIPped by the runner)."
  }
]
