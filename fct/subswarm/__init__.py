"""Subsystem swarm — parallel per-subsystem improvement against the FCP oracle.
See fct/subswarm/README.md. Agents are NAVI SUB-AGENTS spawned via the spawn_agent tool
(NOT Claude Code, NOT tmux) — same model as fct/swarm."""
