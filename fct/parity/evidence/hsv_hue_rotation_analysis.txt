PAEHSVAdjust HUE rotation — decode analysis (2026-07-23)
================================================================================
Node: transfer.PAEHSVAdjust_hue (D23AF030). Hue param id=1. Verdict: CHARACTERIZED —
non-standard rotation, needs the shader HSV-reconstruction + frameSetup disasm, NOT fittable.

PARAM UNIT (decoded, exact): the Hue param is in RADIANS. Measured rotation vs headless FCP
on (200,50,50), anchored EXACTLY at the endpoints:
   param=pi (3.14159) -> 179.7 deg   |  param=2pi (6.28) -> 359.7 deg
So rotation angle = param * 180/pi degrees. The engine's hue-saturation.ts treats Hue as
DEGREES (hg_Params[0].x = Hue/360) — off by 57.3x. (Gate-neutral: all shipping HSV users
author Hue=0.)

But the ROTATION IS NON-STANDARD — every closed-form model fails (worst dR vs headless,
7 inputs x 4 angles = 28 samples):
  - pure HSV hue rotation (turns=param/2pi)      165 dR   (FCP changes sat+val, HSV rot doesn't)
  - verbatim-shader HSV max/min reconstruction   165 dR
  - YIQ chroma rotation (theta=param)            255 dR
  - YCbCr Rec.709 chroma rotation                ~50 dR (best, but structure wrong)
  - Rodrigues about (1,1,1) gray axis            175 dR
  - Rodrigues about luma709 axis                 255 dR
  - unconstrained best-fit LINEAR 3x3 (RGB)       41 dR   <- KEY: not even linear in RGB
  - unconstrained best-fit linear in WS-gamma     49 dR

KEY EVIDENCE: even an unconstrained least-squares 3x3 matrix (the best possible LINEAR map)
leaves 29-41 dR at every angle. So FCP's hue rotation is NONLINEAR in RGB (and in WS-gamma).
This matches the HgcHSVAdjust shader doing a branchy max/min RGB<->HSV round-trip: the
per-pixel sextant reconstruction + the sat/val interaction + clamping is not any single
rotation matrix. FCP output at param=pi/2 on (200,50,50): sat 0.75->0.96, val 0.78->0.36 —
a pure hue rotation preserves both, so the effect is a chroma-plane rotation READ BACK through
the nonlinear HSV path.

CONCLUSION: closing this needs a faithful TS port of the exact HgcHSVAdjust shader (the
max/min hue6 reconstruction, the hue=frac(hue6/6 + offset) with the CORRECT offset unit, and
the HSV->RGB sextant ladder) PLUS the -[PAEHSVAdjust canThrowRenderOutput] frameSetup that
maps the radian param to hg_Params[0].x. Probe-fitting cannot recover a nonlinear branchy
shader. Left CHARACTERIZED (in-gamut small-hue compositions ARE verified via
transfer.PAEHSVAdjust_combined_ingamut). Evidence data: hue_multiinput.json.
