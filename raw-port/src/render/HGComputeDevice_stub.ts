// HGComputeDevice_stub.ts — placeholder for HGComputeDevice (opaque
// Helium class held by shared_ptr in HGRenderQueueSetupProperties'
// device vector @Helium 0x71141 movups load).  Not yet transcribed.

/**
 * HGComputeDevice — Helium class.  Referenced only as
 * `shared_ptr<const HGComputeDevice>` from HGRenderQueueSetupProperties.
 * Layout not yet decoded; treat as opaque.
 *
 * @class Helium HGComputeDevice (not yet transcribed)
 */
export interface HGComputeDevice {
  __opaque: never;
}
