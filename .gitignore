# Build artifacts
oz_render.dylib
*.dylib
*.o

# Python venv (machine-specific; recreate via setup — see README)
venv/

# Browser engine (engine/) node deps
node_modules/

# Render outputs (headless + run_all + engine GT), and large media
out/
all_out/
engine/test/ground-truth/
*.mp4
*.mov

# macOS
.DS_Store

# Python cache
__pycache__/
*.pyc
montage.mp4
tools/__pycache__/
fct/__pycache__/
.fctcache/

# swarm agent scratch (per-worktree prompt files)
.swarm_brief.md
.swarm_reflect_prompt.md
